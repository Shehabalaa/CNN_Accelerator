vsim -gui work.fcmain

add wave -group fcmain  sim:/fcmain/reset sim:/fcmain/clk sim:/fcmain/cnnDone sim:/fcmain/cnnFinishNeoron  sim:/fcmain/cnnNeoron sim:/fcmain/fcDone sim:/fcmain/MAXPrediction

add wave -group MEMORYWeights sim:/fcmain/readRamWeights  sim:/fcmain/finishRamWeights  sim:/fcmain/dataOutRamWeights sim:/fcmain/defaultAddressWeights  sim:/fcmain/incrementWeightAdd sim:/fcmain/dmaAddRamWeights

add wave -group MEMORYNeorons sim:/fcmain/readRamNeorons  sim:/fcmain/finishRamNeorons  sim:/fcmain/dataOutRamNeorons sim:/fcmain/defaultAddressNeorons  sim:/fcmain/incrementNeoronsAdd sim:/fcmain/dmaAddRamNeorons


add wave -group MUL  sim:/fcmain/multiplyWork sim:/fcmain/startMultiply  sim:/fcmain/mulInputWeight  sim:/fcmain/mulInputNeoron


add wave -group numberOFNeorons  sim:/fcmain/decrement sim:/fcmain/loadNumberOFNeorons sim:/fcmain/numberOFNeorons sim:/fcmain/neoronMuxOutput  sim:/fcmain/aluNeoronInput  sim:/fcmain/neoronValueSelection

 


 
add wave -group  State  sim:/fcmain/state

add wave -group Max sim:/fcmain/resetMaxSignal sim:/fcmain/startMax sim:/fcmain/doneMax  sim:/fcmain/labelReg sim:/fcmain/maxNumber





force -freeze sim:/fcmain/clk 1 0, 0 {50 ns} -r 100
force -freeze sim:/fcmain/reset 1 0
force -freeze sim:/fcmain/finishRamWeights 0 0
force -freeze sim:/fcmain/finishRamNeorons 0 0

run 

force -freeze sim:/fcmain/reset 0 0
force -freeze sim:/fcmain/cnnDone 1 0
run
force -freeze sim:/fcmain/cnnDone 0 0

run
run
run

force -freeze sim:/fcmain/finishRamWeights 1 0
force -freeze sim:/fcmain/dataOutRamWeights 80'h0007XXXXXXXXXXXXXXXX 0

run
force -freeze sim:/fcmain/finishRamWeights 0 0
run
run
run
run

force -freeze sim:/fcmain/finishRamWeights 1 0
force -freeze sim:/fcmain/dataOutRamWeights 80'h04eae4ed16e20d00080d 0

run
force -freeze sim:/fcmain/finishRamWeights 0 0
run
run
run
run
run

force -freeze sim:/fcmain/finishRamWeights 1 0
force -freeze sim:/fcmain/finishRamNeorons 1 0
force -freeze sim:/fcmain/dataOutRamWeights 80'hfb04f811e9190a0f060d 0
force -freeze sim:/fcmain/dataOutRamNeorons 80'h0d5cXXXXXXXXXXXXXXXX 0

run


force -freeze sim:/fcmain/finishRamWeights 0 0
force -freeze sim:/fcmain/finishRamNeorons 0 0

run
run
run
